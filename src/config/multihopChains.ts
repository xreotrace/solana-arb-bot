// src/config/multihopChains.ts

import { TOKENS } from "../utils/tokens";

export const MULTIHOP_CHAINS: string[][] = [
  [TOKENS.SOL, TOKENS.USDT, TOKENS.JUP, TOKENS.SOL],
  // [TOKENS.SOL, TOKENS.JUP, TOKENS.USDT, TOKENS.SOL],
  // [TOKENS.SOL, TOKENS.JUP, TOKENS.SOL],
];
